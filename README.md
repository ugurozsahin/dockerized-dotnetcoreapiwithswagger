# Dockerized .NET Core API Application With Swagger 
Dockerized .NET Core API Application With Swagger (Linux)

# Docker Commands
```
docker build . -t dotnetcore01
```

After build

```
docker run -p 8080:5000 dotnetcore01
```